<?xml version="1.0" encoding="utf-8"?>
<!-- name="GENERATOR" content="github.com/mmarkdown/mmark Mmark Markdown Processor - mmark.miek.nl" -->
<rfc version="3" ipr="trust200902" docName="draft-mw-spice-inference-chain-00" submissionType="IETF" category="info" xml:lang="en" xmlns:xi="http://www.w3.org/2001/XInclude" indexInclude="true">

<front>
<title abbrev="SPICE-INFERENCE-CHAIN">Cryptographically Verifiable Inference Chain for AI Agent Computational Provenance</title><seriesInfo value="draft-mw-spice-inference-chain-00" stream="IETF" status="informational" name="Internet-Draft"></seriesInfo>
<author initials="R." surname="Krishnan" fullname="Ram Krishnan"><organization>JPMorgan Chase &amp; Co</organization><address><postal><street></street>
</postal><email>ramkri123@gmail.com</email>
</address></author><author initials="A." surname="Prasad" fullname="A Prasad"><organization>Oracle</organization><address><postal><street></street>
</postal><email>a.prasad@oracle.com</email>
</address></author><author initials="D." surname="Lopez" fullname="Diego R. Lopez"><organization>Telefonica</organization><address><postal><street></street>
</postal><email>diego.r.lopez@telefonica.com</email>
</address></author><author initials="S." surname="Addepalli" fullname="Srinivasa Addepalli"><organization>Aryaka</organization><address><postal><street></street>
</postal><email>srinivasa.addepalli@aryaka.com</email>
</address></author><date year="2026" month="March" day="10"></date>
<area>Security</area>
<workgroup>SPICE</workgroup>
<keyword>inference chain</keyword>
<keyword>spice</keyword>
<keyword>computational provenance</keyword>
<keyword>AI agents</keyword>
<keyword>ZKML</keyword>
<keyword>TEE attestation</keyword>
<keyword>confidential computing</keyword>

<abstract>
<t>This document defines the <tt>inference_root</tt> claim as a companion to the <tt>actor_chain</tt> claim ({{!I-D.draft-mw-spice-actor-chain}}) and the <tt>intent_root</tt> claim ({{!I-D.draft-mw-spice-intent-chain}}). While the actor chain addresses delegation provenance (WHO) and the intent chain addresses content provenance (WHAT), the inference chain addresses computational provenance (HOW) — providing cryptographic proof that a claimed AI model actually performed the inference that produced a given output.</t>
<t>The inference chain leverages two complementary mechanisms: Zero-Knowledge Machine Learning (ZKML) proofs for mathematical certainty, and Trusted Execution Environment (TEE) attestation quotes for production-scale AI workloads. The full inference chain is stored as ordered logs, with only the merkle root included in the OAuth token for efficiency.</t>
<t>Together, the three chains — actor, intent, and inference — form a complete &quot;Truth Stack&quot; for autonomous AI agent governance.</t>
</abstract>

</front>

<middle>

<section anchor="introduction"><name>Introduction</name>

<section anchor="the-problem-computational-spoofing"><name>The Problem: Computational Spoofing</name>
<t>The Actor Chain ({{!I-D.draft-mw-spice-actor-chain}}) proves WHO delegated to whom. The Intent Chain ({{!I-D.draft-mw-spice-intent-chain}}) proves WHAT was produced and how it was transformed. Neither addresses a critical remaining gap: was the <strong>claimed computational process</strong> actually used to produce the output?</t>
<t>In AI agent workflows, an agent may claim to use a high-capability, safety-aligned model (e.g., a frontier model with RLHF alignment) while actually running a cheaper, unaligned model. This &quot;Model Masquerading&quot; attack is undetectable by content hashes alone — the output hash in the intent chain proves the output exists, but not that the claimed model produced it.</t>
<t><strong>Concrete threats:</strong></t>

<ul spacing="compact">
<li><strong>Model Substitution</strong>: A cost-optimized deployment swaps a regulated, audited model for a cheaper alternative with weaker safety properties.</li>
<li><strong>Weight Tampering</strong>: Model weights are modified (e.g., fine-tuned to remove safety guardrails) while the model identifier remains unchanged.</li>
<li><strong>Environment Spoofing</strong>: Inference runs in an unprotected environment where model inputs/outputs can be intercepted, while the agent claims TEE execution.</li>
<li><strong>Replay of Stale Proofs</strong>: A valid inference proof from a previous execution is reattached to a new, unverified output.</li>
</ul>
</section>

<section anchor="relationship-to-actor-chain-and-intent-chain"><name>Relationship to Actor Chain and Intent Chain</name>
<t>This specification completes the three-axis &quot;Truth Stack&quot;:</t>
<table>
<thead>
<tr>
<th align="left">Specification</th>
<th align="left">Axis</th>
<th align="left">Question Answered</th>
<th align="left">STRIDE Coverage</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><strong>Actor Chain</strong> ({{!I-D.draft-mw-spice-actor-chain}})</td>
<td align="left">Identity</td>
<td align="left">WHO delegated to whom?</td>
<td align="left">Spoofing, Repudiation, Elevation of Privilege</td>
</tr>

<tr>
<td align="left"><strong>Intent Chain</strong> ({{!I-D.draft-mw-spice-intent-chain}})</td>
<td align="left">Content</td>
<td align="left">WHAT was produced and transformed?</td>
<td align="left">Repudiation, Tampering</td>
</tr>

<tr>
<td align="left"><strong>Inference Chain</strong> (this document)</td>
<td align="left">Computation</td>
<td align="left">HOW was the output computed?</td>
<td align="left">Spoofing (computational), Tampering (model)</td>
</tr>
</tbody>
</table><table>
<thead>
<tr>
<th align="left">Chain</th>
<th align="left">Plane</th>
<th align="left">Token Content</th>
<th align="left">Full Chain</th>
<th align="left">Primary Consumer</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><strong>Actor</strong></td>
<td align="left">Data Plane</td>
<td align="left">Full chain inline</td>
<td align="left">In token</td>
<td align="left">Every Relying Party (real-time authorization)</td>
</tr>

<tr>
<td align="left"><strong>Intent</strong></td>
<td align="left">Audit Plane</td>
<td align="left">merkle root only</td>
<td align="left">External registry</td>
<td align="left">Audit systems, forensic investigators</td>
</tr>

<tr>
<td align="left"><strong>Inference</strong></td>
<td align="left">Audit Plane</td>
<td align="left">merkle root only</td>
<td align="left">External registry</td>
<td align="left">Auditors, compliance systems</td>
</tr>
</tbody>
</table><t>The three chains are independent and composable:</t>

<ul spacing="compact">
<li><strong>Actor + Intent</strong>: Identity and content governance without computational proof (sufficient for rule-based systems).</li>
<li><strong>Actor + Inference</strong>: Identity and computational governance without content transformation tracking (sufficient for single-agent systems).</li>
<li><strong>All Three</strong>: Full governance for multi-agent pipelines with content transformation and computational integrity.</li>
</ul>
</section>

<section anchor="design-goals"><name>Design Goals</name>

<ol spacing="compact">
<li><strong>Computational Provenance</strong>: Cryptographic proof that a specific model produced a specific output.</li>
<li><strong>Mechanism Agnostic</strong>: Support both ZKML proofs and TEE attestation quotes as proof types.</li>
<li><strong>Binding to Intent Chain</strong>: Inference proofs are bound to specific intent chain entries via content hashes.</li>
<li><strong>Efficiency</strong>: Only merkle root in token; full proofs in registry.</li>
<li><strong>Incremental Adoption</strong>: Deployable alongside existing actor and intent chains without requiring changes to those specifications.</li>
</ol>
</section>
</section>

<section anchor="terminology"><name>Terminology</name>
<t>The key words &quot;MUST&quot;, &quot;MUST NOT&quot;, &quot;REQUIRED&quot;, &quot;SHALL&quot;, &quot;SHALL NOT&quot;, &quot;SHOULD&quot;, &quot;SHOULD NOT&quot;, &quot;RECOMMENDED&quot;, &quot;NOT RECOMMENDED&quot;, &quot;MAY&quot;, and &quot;OPTIONAL&quot; in this document are to be interpreted as described in BCP 14 {{!RFC2119}} {{!RFC8174}} when, and only when, they appear in all capitals, as shown here.</t>

<dl spacing="compact">
<dt>Inference Chain:</dt>
<dd>An ordered sequence of Inference Chain Entries providing cryptographic proof of the computational processes that produced AI agent outputs within a session.</dd>
<dt>Inference Chain Entry:</dt>
<dd>A record binding a specific AI agent output to a cryptographic proof of the computational process that produced it.</dd>
<dt>Inference Root:</dt>
<dd>The merkle root hash of the complete inference chain, included in the OAuth token via the <tt>inference_root</tt> claim.</dd>
<dt>Inference Registry:</dt>
<dd>An append-only ordered log storing the full inference chain entries, partitioned by session.</dd>
<dt>ZKML Proof:</dt>
<dd>A Zero-Knowledge proof (SNARK or STARK) demonstrating that a specific neural network with specific weights produced a specific output from a specific input, without revealing the model weights or input data.</dd>
<dt>TEE Quote:</dt>
<dd>A hardware-signed attestation from a Trusted Execution Environment (e.g., Intel TDX, AMD SEV-SNP, NVIDIA H100 Confidential Computing) proving that specific code ran within a verified enclave with specific measurements. TEE attestation follows the RATS architecture {{!RFC9334}}.</dd>
<dt>Model Fingerprint:</dt>
<dd>A cryptographic hash of the model weights, architecture definition, and inference configuration, uniquely identifying the computational process.</dd>
</dl>
</section>

<section anchor="inference-proof-types"><name>Inference Proof Types</name>

<section anchor="overview"><name>Overview</name>
<t>The inference chain supports two primary proof types, each with different trust assumptions and performance characteristics:</t>
<table>
<thead>
<tr>
<th align="left">Property</th>
<th align="left">ZKML Proof</th>
<th align="left">TEE Quote</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><strong>Trust Basis</strong></td>
<td align="left">Mathematics (cryptographic hardness)</td>
<td align="left">Hardware manufacturer root of trust</td>
</tr>

<tr>
<td align="left"><strong>Verification</strong></td>
<td align="left">Deterministic, no external dependency</td>
<td align="left">Requires manufacturer PKI (Intel/AMD/NVIDIA)</td>
</tr>

<tr>
<td align="left"><strong>Model Scale</strong></td>
<td align="left">Currently limited (~100M parameters)</td>
<td align="left">Production-scale LLMs (100B+ parameters)</td>
</tr>

<tr>
<td align="left"><strong>Proof Generation</strong></td>
<td align="left">Minutes to hours per inference</td>
<td align="left">Real-time (millisecond overhead)</td>
</tr>

<tr>
<td align="left"><strong>Proof Size</strong></td>
<td align="left">~200 bytes (Groth16), ~50KB (STARKs)</td>
<td align="left">~2KB (Intel TDX), ~4KB (NVIDIA)</td>
</tr>

<tr>
<td align="left"><strong>Privacy</strong></td>
<td align="left">Zero-knowledge: hides model weights</td>
<td align="left">Enclave-bound: hardware isolation</td>
</tr>
</tbody>
</table><t>Deployments SHOULD select the proof type based on their performance requirements and trust model.</t>
</section>

<section anchor="zkml-proofs"><name>ZKML Proofs</name>
<t>ZKML proofs provide mathematical certainty that a specific model produced a specific output. They are constructed by encoding the neural network's forward pass as an arithmetic circuit and generating a zero-knowledge proof of correct execution.</t>
<t><strong>Proof Statement</strong>: &quot;There exist model weights W such that hash(W) = model_fingerprint AND forward_pass(W, input) = output.&quot;</t>
<t><strong>Properties</strong>:</t>

<ul spacing="compact">
<li>The verifier learns nothing about the model weights (zero-knowledge property).</li>
<li>The proof cannot be forged without performing the actual computation (soundness).</li>
<li>Proof verification is fast (milliseconds) regardless of model size.</li>
<li>Proof generation is computationally expensive (orders of magnitude slower than inference).</li>
</ul>
<t><strong>Applicable Proof Systems</strong>:</t>
<table>
<thead>
<tr>
<th align="left">System</th>
<th align="left">Proof Size</th>
<th align="left">Verification Time</th>
<th align="left">Trusted Setup</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left">Groth16</td>
<td align="left">~200 bytes</td>
<td align="left"><sub>5ms</sub></td>
<td align="left">Required (per-circuit)</td>
</tr>

<tr>
<td align="left">PLONK</td>
<td align="left">~400 bytes</td>
<td align="left"><sub>10ms</sub></td>
<td align="left">Universal (one-time)</td>
</tr>

<tr>
<td align="left">STARKs</td>
<td align="left"><sub>50KB</sub></td>
<td align="left"><sub>50ms</sub></td>
<td align="left">None (transparent)</td>
</tr>

<tr>
<td align="left">Halo2</td>
<td align="left"><sub>5KB</sub></td>
<td align="left"><sub>15ms</sub></td>
<td align="left">None (recursive)</td>
</tr>
</tbody>
</table><t><strong>Entry Structure</strong>:</t>

<sourcecode type="json"><![CDATA[{
  "type": "zkml_proof",
  "sub": "spiffe://example.com/agent/analyst",
  "proof_system": "groth16",
  "model_fingerprint": "sha256:weights_hash...",
  "model_id": "analyst-model-v3.2",
  "input_hash": "sha256:input_data...",
  "output_hash": "sha256:output_data...",
  "intent_entry_ref": 3,
  "proof": "base64:proof_bytes...",
  "verification_key_hash": "sha256:vk_hash...",
  "verification_key_registry":
    "https://vk-registry.example.com/keys/analyst-v3.2",
  "iat": 1700000030,
  "inference_digest": "sha256:...",
  "inference_sig": "eyJhbGci..."
}
]]></sourcecode>
</section>

<section anchor="tee-attestation-quotes"><name>TEE Attestation Quotes</name>
<t>TEE attestation quotes provide hardware-rooted proof that inference was performed within a verified enclave. The hardware generates a signed &quot;quote&quot; containing measurements of the code, data, and configuration running inside the enclave.</t>
<t><strong>Proof Statement</strong>: &quot;Hardware H with platform certificate C attests that enclave E with measurements M produced output O from input I.&quot;</t>
<t><strong>Properties</strong>:</t>

<ul spacing="compact">
<li>Proof is generated in real-time with minimal overhead.</li>
<li>Verification requires the hardware manufacturer's PKI (root certificate).</li>
<li>The quote binds the computation to specific hardware and firmware versions.</li>
<li>The enclave protects model weights and inference data from the host OS.</li>
</ul>
<t><strong>Supported Platforms</strong>:</t>
<table>
<thead>
<tr>
<th align="left">Platform</th>
<th align="left">TEE Technology</th>
<th align="left">GPU Support</th>
<th align="left">Quote Format</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left">Intel</td>
<td align="left">TDX (Trust Domain Extensions)</td>
<td align="left">Via NVIDIA H100 CC</td>
<td align="left">DCAP Quote v4</td>
</tr>

<tr>
<td align="left">AMD</td>
<td align="left">SEV-SNP</td>
<td align="left">Via NVIDIA H100 CC</td>
<td align="left">Attestation Report</td>
</tr>

<tr>
<td align="left">NVIDIA</td>
<td align="left">H100 Confidential Computing</td>
<td align="left">Native</td>
<td align="left">GPU Attestation</td>
</tr>

<tr>
<td align="left">ARM</td>
<td align="left">CCA (Confidential Compute Architecture)</td>
<td align="left">Planned</td>
<td align="left">CCA Token</td>
</tr>
</tbody>
</table><t><strong>Entry Structure</strong>:</t>

<sourcecode type="json"><![CDATA[{
  "type": "tee_attestation",
  "sub": "spiffe://example.com/agent/analyst",
  "platform": "nvidia_h100_cc",
  "model_fingerprint": "sha256:weights_hash...",
  "model_id": "analyst-model-v3.2",
  "input_hash": "sha256:input_data...",
  "output_hash": "sha256:output_data...",
  "intent_entry_ref": 3,
  "quote": {
    "format": "nvidia_gpu_attestation_v1",
    "enclave_measurement": "sha384:enclave_mr...",
    "firmware_version": "H100.96.00.5E.00.01",
    "platform_cert_chain": [
      "base64:nvidia_device_cert...",
      "base64:nvidia_intermediate_cert..."
    ],
    "report_data": "sha256:binding_hash...",
    "signature": "base64:hw_signature..."
  },
  "por_ref": "spiffe://example.com/wia/gpu-node-7",
  "iat": 1700000030,
  "inference_digest": "sha256:...",
  "inference_sig": "eyJhbGci..."
}
]]></sourcecode>
</section>

<section anchor="hybrid-proofs"><name>Hybrid Proofs</name>
<t>For high-assurance deployments, both proof types MAY be combined for a single inference operation:</t>

<ul spacing="compact">
<li><strong>TEE quote</strong> provides real-time proof that inference ran in a protected enclave.</li>
<li><strong>ZKML proof</strong> provides mathematical proof of correct computation (generated asynchronously after inference).</li>
</ul>
<t>The inference chain entry for a hybrid proof references both:</t>

<sourcecode type="json"><![CDATA[{
  "type": "hybrid_proof",
  "sub": "spiffe://example.com/agent/analyst",
  "tee_entry_ref": 4,
  "zkml_entry_ref": 5,
  "model_fingerprint": "sha256:weights_hash...",
  "output_hash": "sha256:output_data...",
  "intent_entry_ref": 3,
  "iat": 1700000035,
  "inference_digest": "sha256:...",
  "inference_sig": "eyJhbGci..."
}
]]></sourcecode>
</section>
</section>

<section anchor="entry-structure"><name>Entry Structure</name>

<section anchor="common-fields"><name>Common Fields</name>
<t>All inference chain entries share common fields:</t>
<table>
<thead>
<tr>
<th align="left">Field</th>
<th align="left">Type</th>
<th align="left">Required</th>
<th align="left">Description</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><tt>type</tt></td>
<td align="left">string</td>
<td align="left">REQUIRED</td>
<td align="left">Entry type: <tt>zkml_proof</tt>, <tt>tee_attestation</tt>, <tt>hybrid_proof</tt></td>
</tr>

<tr>
<td align="left"><tt>sub</tt></td>
<td align="left">string</td>
<td align="left">REQUIRED</td>
<td align="left">SPIFFE ID of the agent that performed inference</td>
</tr>

<tr>
<td align="left"><tt>model_fingerprint</tt></td>
<td align="left">string</td>
<td align="left">REQUIRED</td>
<td align="left">SHA-256 hash of model weights + architecture</td>
</tr>

<tr>
<td align="left"><tt>model_id</tt></td>
<td align="left">string</td>
<td align="left">REQUIRED</td>
<td align="left">Human-readable model identifier and version</td>
</tr>

<tr>
<td align="left"><tt>output_hash</tt></td>
<td align="left">string</td>
<td align="left">REQUIRED</td>
<td align="left">SHA-256 hash of inference output (MUST match intent chain entry)</td>
</tr>

<tr>
<td align="left"><tt>intent_entry_ref</tt></td>
<td align="left">number</td>
<td align="left">REQUIRED</td>
<td align="left">Index of corresponding entry in intent chain</td>
</tr>

<tr>
<td align="left"><tt>iat</tt></td>
<td align="left">number</td>
<td align="left">REQUIRED</td>
<td align="left">Timestamp when proof was generated</td>
</tr>

<tr>
<td align="left"><tt>inference_digest</tt></td>
<td align="left">string</td>
<td align="left">REQUIRED</td>
<td align="left">Cumulative hash for merkle tree</td>
</tr>

<tr>
<td align="left"><tt>inference_sig</tt></td>
<td align="left">string</td>
<td align="left">REQUIRED</td>
<td align="left">Signature over <tt>inference_digest</tt></td>
</tr>
</tbody>
</table><t>The <tt>sub</tt> field identifies the agent that performed inference. It corresponds to the <tt>sub</tt> field of the matching actor chain entry ({{!I-D.draft-mw-spice-actor-chain}}).</t>
</section>

<section anchor="binding-to-intent-chain"><name>Binding to Intent Chain</name>
<t>The <tt>intent_entry_ref</tt> field binds each inference proof to a specific intent chain entry. The <tt>output_hash</tt> in the inference entry MUST match the <tt>output_hash</tt> (for agent outputs) in the referenced intent chain entry. This binding ensures that the computational proof corresponds to the actual content recorded in the intent chain.</t>

<artwork><![CDATA[Intent Chain:                 Inference Chain:
┌──────────────────┐         ┌──────────────────┐
│ Entry 0          │         │                  │
│ type: non_det    │◄────────│ Entry 0          │
│ output: sha:abc  │         │ type: tee_quote  │
│                  │         │ output: sha:abc  │
│                  │         │ intent_ref: 0    │
└──────────────────┘         └──────────────────┘
┌──────────────────┐
│ Entry 1          │         (no proof needed
│ type: determ.    │          for deterministic
│                  │          filters)
└──────────────────┘
┌──────────────────┐         ┌──────────────────┐
│ Entry 3          │         │                  │
│ type: non_det    │◄────────│ Entry 1          │
│ output: sha:def  │         │ type: zkml       │
│                  │         │ output: sha:def  │
│                  │         │ intent_ref: 3    │
└──────────────────┘         └──────────────────┘
]]></artwork>
</section>
</section>

<section anchor="storage-architecture"><name>Storage Architecture</name>

<section anchor="inference-registry"><name>Inference Registry</name>
<t>The inference registry stores immutable inference chain entries as ordered logs, following the same architecture as the intent registry.</t>
<t><strong>Properties</strong>:</t>

<ul spacing="compact">
<li>Append-only (immutable)</li>
<li>Ordered by offset within session</li>
<li>Partitioned by session ID</li>
<li>Eventual consistency acceptable</li>
</ul>
<blockquote><t>Inference registry entries MUST NOT contain OAuth tokens, bearer credentials, or signing keys. Entries contain only inference proofs, content hashes, metadata, and agent identities. The token references the registry via the <tt>inference_registry</tt> claim; the registry MUST NOT store or reference the token itself.</t>
</blockquote><t>Implementations SHOULD use an append-only log with tamper-evident guarantees and partitioned, ordered retrieval by session ID.</t>
<t><strong>Log Structure</strong>:</t>

<sourcecode type="json"><![CDATA[{
  "session_id": "sess-uuid-12345",
  "offset": 0,
  "entry": {
    "type": "tee_attestation",
    "sub": "spiffe://example.com/agent/A",
    "model_fingerprint": "sha256:weights...",
    "model_id": "agent-A-model-v2",
    "output_hash": "sha256:abc...",
    "intent_entry_ref": 0,
    "quote": { "..." : "..." },
    "iat": 1700000010,
    "inference_digest": "sha256:...",
    "inference_sig": "eyJ..."
  }
}
]]></sourcecode>
</section>

<section anchor="merkle-tree-construction"><name>merkle Tree Construction</name>
<t>The inference chain merkle tree follows the same construction algorithm as the intent chain (defined in {{!I-D.draft-mw-spice-intent-chain}} Section 5.3). Leaf nodes are the SHA-256 hashes of canonically serialized inference chain entries. The resulting root hash is included in the OAuth token as the <tt>inference_root</tt> claim.</t>
</section>

<section anchor="merkle-root-in-token"><name>merkle Root in Token</name>
<t>Only the merkle root is included in the OAuth token:</t>

<sourcecode type="json"><![CDATA[{
  "inference_root": "sha256:xyz789...",
  "inference_proof_type": "groth16",
  "inference_registry":
    "https://proof-log.example.com"
}
]]></sourcecode>
<table>
<thead>
<tr>
<th align="left">Field</th>
<th align="left">Type</th>
<th align="left">Required</th>
<th align="left">Description</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><tt>inference_root</tt></td>
<td align="left">string</td>
<td align="left">REQUIRED</td>
<td align="left">merkle root hash of inference chain</td>
</tr>

<tr>
<td align="left"><tt>inference_proof_type</tt></td>
<td align="left">string</td>
<td align="left">OPTIONAL</td>
<td align="left">Primary proof algorithm used (e.g., <tt>groth16</tt>, <tt>tee_tdx</tt>, <tt>hybrid</tt>)</td>
</tr>

<tr>
<td align="left"><tt>inference_registry</tt></td>
<td align="left">string</td>
<td align="left">REQUIRED</td>
<td align="left">URI of inference registry for proof retrieval</td>
</tr>
</tbody>
</table></section>
</section>

<section anchor="token-structure"><name>Token Structure</name>

<section anchor="combined-token-format-full-truth-stack"><name>Combined Token Format (Full Truth Stack)</name>
<t>The complete token combines session, actor chain, intent chain, and inference chain:</t>

<sourcecode type="json"><![CDATA[{
  "iss": "https://auth.example.com",
  "sub": "user-alice",
  "aud": "https://api.example.com",
  "jti": "tok-eee-12345",
  "sid": "sess-uuid-12345",
  "iat": 1700000000,
  "exp": 1700003600,

  "session": {
    "session_id": "sess-uuid-12345",
    "type": "human_initiated",
    "initiator": "user-alice",
    "approval_ref": "approval-uuid-789",
    "max_chain_depth": 5
  },

  "actor_chain": [
    {
      "sub":
        "spiffe://example.com/agent/orchestrator",
      "iss": "https://auth.example.com",
      "iat": 1700000010
    },
    {
      "sub":
        "spiffe://example.com/agent/analyst",
      "iss": "https://auth.example.com",
      "iat": 1700000030
    }
  ],

  "intent_root": "sha256:abc123...",
  "intent_registry":
    "https://intent-log.example.com/sessions/sess-uuid-12345",

  "inference_root": "sha256:xyz789...",
  "inference_proof_type": "tee_h100",
  "inference_registry":
    "https://proof-log.example.com/sessions/sess-uuid-12345"
}
]]></sourcecode>
</section>

<section anchor="claim-definitions"><name>Claim Definitions</name>
<table>
<thead>
<tr>
<th align="left">Claim</th>
<th align="left">Type</th>
<th align="left">Description</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><tt>inference_root</tt></td>
<td align="left">string</td>
<td align="left">merkle root hash of inference chain</td>
</tr>

<tr>
<td align="left"><tt>inference_proof_type</tt></td>
<td align="left">string</td>
<td align="left">Primary proof algorithm (e.g., <tt>groth16</tt>, <tt>tee_tdx</tt>, <tt>tee_h100</tt>, <tt>hybrid</tt>)</td>
</tr>

<tr>
<td align="left"><tt>inference_registry</tt></td>
<td align="left">string</td>
<td align="left">URI for retrieving full inference chain or proofs (REQUIRED)</td>
</tr>
</tbody>
</table></section>
</section>

<section anchor="verification-procedures"><name>Verification Procedures</name>

<section anchor="three-point-verification"><name>Three-Point Verification</name>
<t>A Relying Party receiving a token with all three chains performs a &quot;Three-Point Check&quot;:</t>

<ol spacing="compact">
<li><strong>Identity</strong> (Actor Chain): Is the delegation chain valid? Are all actors authorized?</li>
<li><strong>Integrity</strong> (Intent Chain): Does the content hash trail match the <tt>intent_root</tt>?</li>
<li><strong>Validity</strong> (Inference Chain): Does the computational proof verify against the <tt>inference_root</tt>?</li>
</ol>
</section>

<section anchor="zkml-proof-verification"><name>ZKML Proof Verification</name>
<t>To verify a ZKML inference entry:</t>

<ol spacing="compact">
<li>Retrieve the entry from the inference registry.</li>
<li>Fetch the verification key from <tt>verification_key_registry</tt>.</li>
<li>Verify <tt>hash(verification_key) == verification_key_hash</tt>.</li>
<li>Run the ZKP verifier: <tt>verify(proof, verification_key, public_inputs)</tt> where <tt>public_inputs = (model_fingerprint, input_hash, output_hash)</tt>.</li>
<li>Verify <tt>output_hash</tt> matches the corresponding intent chain entry.</li>
</ol>
</section>

<section anchor="tee-quote-verification"><name>TEE Quote Verification</name>
<t>To verify a TEE attestation entry:</t>

<ol spacing="compact">
<li>Retrieve the entry from the inference registry.</li>
<li>Validate the platform certificate chain against the manufacturer's root CA (e.g., Intel, NVIDIA).</li>
<li>Check the platform certificate has not been revoked (TCB recovery check).</li>
<li>Verify the quote signature using the platform certificate.</li>
<li>Verify <tt>enclave_measurement</tt> matches the expected measurement for the claimed model.</li>
<li>Verify <tt>report_data</tt> binds the quote to the specific input/output hashes.</li>
<li>Verify <tt>output_hash</tt> matches the corresponding intent chain entry.</li>
</ol>
</section>

<section anchor="tiered-verification"><name>Tiered Verification</name>
<t>Inference verification extends the tiered strategy from {{!I-D.draft-mw-spice-intent-chain}}:</t>
<table>
<thead>
<tr>
<th align="left">Risk Level</th>
<th align="left">Actor Chain</th>
<th align="left">Intent Chain</th>
<th align="left">Inference Chain</th>
<th align="left">Use Case</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left">Low</td>
<td align="left">Sync</td>
<td align="left">Skip</td>
<td align="left">Skip</td>
<td align="left">Read operations</td>
</tr>

<tr>
<td align="left">Medium</td>
<td align="left">Sync</td>
<td align="left">Cached proof</td>
<td align="left">Skip</td>
<td align="left">Create/update</td>
</tr>

<tr>
<td align="left">High</td>
<td align="left">Sync</td>
<td align="left">Full</td>
<td align="left">TEE quote check</td>
<td align="left">Financial decisions</td>
</tr>

<tr>
<td align="left">Critical</td>
<td align="left">Sync</td>
<td align="left">Full</td>
<td align="left">Full ZKML + TEE</td>
<td align="left">Regulatory, high-stakes</td>
</tr>
</tbody>
</table></section>
</section>

<section anchor="policy-enforcement"><name>Policy Enforcement</name>

<section anchor="policy-examples"><name>Policy Examples</name>

<section anchor="require-inference-proof-for-all-agent-outputs"><name>Require Inference Proof for All Agent Outputs</name>

<artwork><![CDATA[require_inference_proofs {
    intent_chain := get_intent_chain(
        input.intent_root)
    inference_chain := get_inference_chain(
        input.inference_root)

    agent_outputs := [i |
        intent_chain[i].type == "non_deterministic"]

    every i in agent_outputs {
        some j
        inference_chain[j].intent_entry_ref == i
    }
}
]]></artwork>
</section>

<section anchor="require-tee-for-financial-operations"><name>Require TEE for Financial Operations</name>

<artwork><![CDATA[require_tee_for_finance {
    input.action in ["transfer", "approve",
                      "underwrite"]

    inference_chain := get_inference_chain(
        input.inference_root)

    every entry in inference_chain {
        entry.type in ["tee_attestation",
                       "hybrid_proof"]
    }
}
]]></artwork>
</section>

<section anchor="block-specific-model-versions"><name>Block Specific Model Versions</name>

<artwork><![CDATA[block_deprecated_models {
    inference_chain := get_inference_chain(
        input.inference_root)

    every entry in inference_chain {
        not entry.model_id in
            data.deprecated_models
    }
}
]]></artwork>
</section>
</section>

<section anchor="integration-with-policy-engines"><name>Integration with Policy Engines</name>
<t>The inference chain claims are designed for consumption by policy engines such as Open Policy Agent (OPA). A policy engine SHOULD:</t>

<ol spacing="compact">
<li>Verify actor chain integrity (per {{!I-D.draft-mw-spice-actor-chain}}).</li>
<li>Verify intent chain integrity (per {{!I-D.draft-mw-spice-intent-chain}}).</li>
<li>Verify <tt>inference_root</tt> and <tt>inference_registry</tt> are present and non-empty.</li>
<li>Evaluate deployment-specific requirements such as proof type (ZKML vs TEE), model version policies, and hardware platform requirements.</li>
</ol>
</section>
</section>

<section anchor="security-considerations"><name>Security Considerations</name>

<section anchor="stride-analysis-for-inference-chain"><name>STRIDE Analysis for Inference Chain</name>
<table>
<thead>
<tr>
<th align="left">Threat</th>
<th align="left">Attack</th>
<th align="left">Mitigation</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><strong>Spoofing</strong> (Computational)</td>
<td align="left">Model substitution</td>
<td align="left"><tt>model_fingerprint</tt> binds proof to specific weights</td>
</tr>

<tr>
<td align="left"><strong>Tampering</strong> (Model)</td>
<td align="left">Weight modification</td>
<td align="left">Hash of weights in proof statement</td>
</tr>

<tr>
<td align="left"><strong>Repudiation</strong></td>
<td align="left">&quot;I didn't run that model&quot;</td>
<td align="left">Signed inference entries with agent SPIFFE ID</td>
</tr>

<tr>
<td align="left"><strong>Replay</strong></td>
<td align="left">Reuse old proof for new output</td>
<td align="left"><tt>input_hash</tt> + <tt>output_hash</tt> binding + <tt>iat</tt> timestamp</td>
</tr>

<tr>
<td align="left"><strong>Environment Spoofing</strong></td>
<td align="left">Claim TEE without using one</td>
<td align="left">TEE quote with hardware-signed measurements</td>
</tr>
</tbody>
</table></section>

<section anchor="trust-assumptions"><name>Trust Assumptions</name>

<section anchor="zkml-proofs-1"><name>ZKML Proofs</name>

<ul spacing="compact">
<li>Trust the cryptographic hardness assumptions of the proof system (e.g., discrete log for Groth16, collision resistance for STARKs).</li>
<li>Trust the correctness of the arithmetic circuit encoding the neural network.</li>
<li>DO NOT require trust in any hardware manufacturer.</li>
</ul>
</section>

<section anchor="tee-attestation"><name>TEE Attestation</name>

<ul spacing="compact">
<li>Trust the hardware manufacturer's root of trust (Intel, AMD, NVIDIA).</li>
<li>Trust the manufacturer's TCB recovery process (firmware updates, revocation).</li>
<li>Trust that the TEE hardware correctly isolates the enclave from the host.</li>
<li>ACKNOWLEDGE the risk of hardware-level attacks (side channels, fault injection) although these require physical access and are outside the typical threat model.</li>
</ul>
</section>
</section>

<section anchor="verification-key-management"><name>Verification Key Management</name>
<t>For ZKML proofs, the verification key uniquely identifies the circuit (and therefore the model architecture). Verification keys:</t>

<ul spacing="compact">
<li>MUST be published in a discoverable registry (<tt>verification_key_registry</tt>).</li>
<li>MUST be versioned alongside model versions.</li>
<li>SHOULD be pinned by deployers to prevent key substitution attacks.</li>
<li>MAY be registered in a SCITT transparency log for long-term auditability.</li>
</ul>
</section>

<section anchor="proof-freshness"><name>Proof Freshness</name>
<t>Inference proofs MUST include an <tt>iat</tt> timestamp. Verifiers SHOULD reject proofs older than a configurable maximum age. For TEE quotes, the platform firmware version SHOULD be checked against the manufacturer's latest TCB baseline to ensure the hardware is patched.</t>
</section>

<section anchor="credential-isolation"><name>Credential Isolation</name>
<t>Inference registry entries MUST NOT contain OAuth tokens, bearer credentials, or signing keys. The relationship between tokens and registry entries is one-directional: the token references the registry via the <tt>inference_registry</tt> URI claim, but the registry MUST NOT store or reference the token. This separation ensures that compromise of the inference registry does not expose bearer credentials.</t>
</section>

<section anchor="zkml-proof-size-considerations"><name>ZKML Proof Size Considerations</name>
<t>STARK-based proofs can be tens of kilobytes. The merkle tree architecture ensures this does not affect token size (only the root hash is in the JWT). However, deployments using STARKs SHOULD consider:</t>

<ul spacing="compact">
<li>Pre-computing and caching proofs for frequently-used model/input combinations.</li>
<li>Using recursive proof composition to compress multiple STARK proofs into a single succinct proof.</li>
<li>Implementing proof compression techniques specific to their proof system.</li>
</ul>
</section>

<section anchor="relationship-to-proof-of-residency"><name>Relationship to Proof of Residency</name>
<t>The inference chain's TEE attestation entries are complementary to, but distinct from, the Proof of Residency (PoR) in actor chain entries ({{!I-D.draft-mw-spice-transitive-attestation}}):</t>

<ul spacing="compact">
<li><strong>PoR</strong> (in actor chain): Proves the agent's IDENTITY is bound to a TEE. Answers &quot;Is this agent running in a secure environment?&quot;</li>
<li><strong>TEE Quote</strong> (in inference chain): Proves a specific COMPUTATION ran in a TEE. Answers &quot;Did this model execution happen inside an enclave?&quot;</li>
</ul>
<t>Both MAY reference the same underlying hardware, but serve different verification purposes. The <tt>por_ref</tt> field in TEE attestation entries allows cross-referencing.</t>
</section>
</section>

<section anchor="privacy-considerations"><name>Privacy Considerations</name>

<section anchor="model-weight-privacy"><name>Model Weight Privacy</name>
<t>ZKML proofs provide zero-knowledge privacy for model weights — the verifier learns that the output was produced by a model with a specific fingerprint, but learns nothing about the actual weights. This is valuable for proprietary models where the weights are confidential.</t>
<t>TEE attestation provides enclave isolation — the model weights are protected from the host OS, but the enclave measurement reveals information about the model binary.</t>
</section>

<section anchor="input-privacy"><name>Input Privacy</name>
<t>Both proof types include <tt>input_hash</tt> rather than the raw input, providing input privacy by default. For applications requiring stronger guarantees, ZKML proofs can be constructed to hide the input entirely (proving only that &quot;some valid input&quot; produced the output).</t>
</section>

<section anchor="selective-disclosure"><name>Selective Disclosure</name>
<t>Inference chain entries MAY use Selective Disclosure (SD-JWT) {{!I-D.ietf-oauth-selective-disclosure-jwt}} to hide sensitive fields such as <tt>model_id</tt> or <tt>platform</tt> from certain verifiers while maintaining proof integrity.</t>
</section>
</section>

<section anchor="implementation-guidance"><name>Implementation Guidance</name>

<section anchor="current-zkml-landscape"><name>Current ZKML Landscape</name>
<t>As of this writing, ZKML is practical for models up to approximately 100 million parameters. Active research is scaling this boundary through optimized circuit designs and recursive proof composition.</t>
<t>Deployments SHOULD plan for ZKML scalability improvements and MAY use TEE attestation as a near-term bridge.</t>
</section>

<section anchor="tee-integration"><name>TEE Integration</name>
<t>For TEE-based inference proofs:</t>

<ol spacing="compact">
<li>Deploy the model inside a TEE enclave (e.g., NVIDIA Confidential Computing on H100).</li>
<li>At inference time, generate a hardware quote binding the input hash and output hash to the enclave measurement.</li>
<li>Append the quote as a TEE attestation entry to the inference registry.</li>
<li>The quote's <tt>report_data</tt> field MUST contain <tt>sha256(input_hash || output_hash)</tt> to bind the proof to specific content.</li>
</ol>
</section>

<section anchor="scalability-considerations"><name>Scalability Considerations</name>

<ul spacing="compact">
<li><strong>Async ZKML</strong>: ZKML proofs MAY be generated asynchronously after inference. The intent chain entry is appended immediately; the inference chain entry is appended when the proof is ready. The merkle root is recomputed at token exchange time.</li>
<li><strong>Proof Batching</strong>: Multiple inference operations MAY be batched into a single ZKML proof using recursive composition, reducing verification overhead.</li>
<li><strong>Quote Caching</strong>: TEE quotes for the same enclave measurement MAY be cached for a configurable period, reducing quote generation overhead.</li>
</ul>
</section>

<section anchor="multi-as-deployments"><name>Multi-AS Deployments</name>
<t>In deployments involving multiple Authorization Servers, the inference registry is shared across all participating ASes under the same session partition. Each AS appends inference chain entries under the <tt>sid</tt> partition established at session initiation. The <tt>inference_root</tt> in each successive token is recomputed over all proof entries accumulated so far — it therefore differs at each hop as the chain grows. This is expected behavior: a relying party receiving a later token will see a larger <tt>inference_root</tt> than one receiving an earlier token in the same session. ASes do not need to share keys or coordinate directly — the session partition and append-only log semantics provide the necessary consistency, following the same pattern as the Actor Chain Registry ({{!I-D.draft-mw-spice-actor-chain}}) and Intent Registry ({{!I-D.draft-mw-spice-intent-chain}}).</t>
</section>

<section anchor="registry-availability"><name>Registry Availability</name>
<t>Inference registry unavailability does not affect data-plane operation — the token's AS-signed <tt>inference_root</tt> is sufficient for request-time policy decisions. Per-entry proof verification is deferred to the audit plane.</t>
<t>However, inference proofs are uniquely valuable because they cannot be regenerated after the fact — the exact model state, input, and execution environment may no longer be available. Deployments SHOULD:</t>

<ul spacing="compact">
<li>Replicate inference registry entries across availability zones.</li>
<li>Use append-only log services designed for high durability (e.g., SCITT transparency logs).</li>
<li>Define a fail-mode policy: <strong>fail-closed</strong> for regulated workloads (e.g., financial, healthcare), or <strong>fail-open</strong> with verification gap logging for lower-risk workloads.</li>
</ul>
<t>A federated IAM/IdM platform (e.g., Keycloak, Microsoft Entra, Okta, PingFederate) MAY host the inference registry alongside the Actor Chain Registry and Intent Registry under the same session partition — see {{!I-D.draft-mw-spice-actor-chain}} Section &quot;Registry Hosting&quot; for detailed requirements.</t>

<section anchor="proof-size-and-storage"><name>Proof Size and Storage</name>
<t>Inference proof payloads are significantly larger than actor chain entries (~0.5-1KB) or intent chain entries (<sub>0.5-1KB):</sub></t>
<table>
<thead>
<tr>
<th align="left">Proof Type</th>
<th align="left">Typical Size</th>
<th align="left">Storage Implication</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left">TEE attestation quote</td>
<td align="left"><sub>2-4KB</sub></td>
<td align="left">Compatible with most IAM data stores</td>
</tr>

<tr>
<td align="left">Groth16 ZK proof</td>
<td align="left">~200 bytes</td>
<td align="left">Compatible with most IAM data stores</td>
</tr>

<tr>
<td align="left">STARK proof</td>
<td align="left"><sub>50KB</sub></td>
<td align="left">May exceed IAM data store blob limits</td>
</tr>
</tbody>
</table><t>Most IAM/IdM platforms are not designed to store large binary blobs. Deployments that include STARK proofs SHOULD either:</t>

<ul spacing="compact">
<li>Use a dedicated object store (e.g., S3, GCS, Azure Blob Storage) for proof payloads, with the IAM platform serving as the registry index and access control layer.</li>
<li>Or use a SCITT transparency log that natively supports variable-size entries.</li>
</ul>
</section>
</section>

<section anchor="performance-guidance-tee-vs-zkml"><name>Performance Guidance: TEE vs ZKML</name>
<t>Deployments SHOULD select proof types based on their latency requirements:</t>

<ul spacing="compact">
<li><strong>Real-time applications</strong> (chat, interactive agents, API serving): Use TEE attestation quotes exclusively. TEE proof generation adds millisecond-level overhead and is compatible with production-scale LLMs (100B+ parameters).</li>
<li><strong>Batch/offline applications</strong> (document generation, model evaluation, regulatory reporting): ZKML proofs provide mathematical certainty independent of hardware trust. Proof generation latency (minutes to hours) is acceptable when not on the critical path.</li>
<li><strong>Hybrid deployments</strong>: Use TEE quotes for real-time inference and generate ZKML proofs asynchronously for high-value operations, appending them to the inference chain after the fact. The merkle root is recomputed at the next token exchange.</li>
</ul>
</section>
</section>

<section anchor="design-rationale-merkle-root-in-token"><name>Design Rationale: merkle Root in Token</name>
<t>The inference chain follows the same merkle root architecture as the intent chain (see {{!I-D.draft-mw-spice-intent-chain}} Design Rationale for the detailed comparison). The same trade-offs apply, with additional motivation: inference proofs are large (STARK proofs ~50KB, TEE quotes ~2-4KB), making inline embedding in tokens impractical. The merkle root enables selective verification of individual proofs using O(log n) sibling hashes, which is critical for the tiered verification strategy.</t>
</section>

<section anchor="iana-considerations"><name>IANA Considerations</name>

<section anchor="jwt-claim-registration"><name>JWT Claim Registration</name>
<t>This document requests registration of the following claims in the &quot;JSON Web Token Claims&quot; registry established by {{!RFC7519}}:</t>

<ul>
<li><strong>Claim Name</strong>: <tt>inference_root</tt></li>
<li><strong>Claim Description</strong>: merkle root hash of the inference chain for computational provenance verification.</li>
<li><strong>Change Controller</strong>: IETF</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
<li><t><strong>Claim Name</strong>: <tt>inference_proof_type</tt></t>
</li>
<li><t><strong>Claim Description</strong>: Primary proof algorithm used in the inference chain.</t>
</li>
<li><t><strong>Change Controller</strong>: IETF</t>
</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
<li><t><strong>Claim Name</strong>: <tt>inference_registry</tt></t>
</li>
<li><t><strong>Claim Description</strong>: URI of the inference registry for proof retrieval.</t>
</li>
<li><t><strong>Change Controller</strong>: IETF</t>
</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
</ul>
</section>

<section anchor="cwt-claim-registration"><name>CWT Claim Registration</name>
<t>This document requests registration of the following claims in the &quot;CBOR Web Token (CWT) Claims&quot; registry established by {{!RFC8392}}:</t>

<ul>
<li><strong>Claim Name</strong>: <tt>inference_root</tt></li>
<li><strong>Claim Description</strong>: merkle root hash of the inference chain.</li>
<li><strong>CBOR Key</strong>: TBD (e.g., 60)</li>
<li><strong>Claim Type</strong>: tstr</li>
<li><strong>Change Controller</strong>: IETF</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
<li><t><strong>Claim Name</strong>: <tt>inference_registry</tt></t>
</li>
<li><t><strong>Claim Description</strong>: URI of the inference registry for proof retrieval.</t>
</li>
<li><t><strong>CBOR Key</strong>: TBD (e.g., 61)</t>
</li>
<li><t><strong>Claim Type</strong>: tstr</t>
</li>
<li><t><strong>Change Controller</strong>: IETF</t>
</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
<li><t><strong>Claim Name</strong>: <tt>inference_proof_type</tt></t>
</li>
<li><t><strong>Claim Description</strong>: Primary proof algorithm used in the inference chain.</t>
</li>
<li><t><strong>CBOR Key</strong>: TBD (e.g., 62)</t>
</li>
<li><t><strong>Claim Type</strong>: tstr</t>
</li>
<li><t><strong>Change Controller</strong>: IETF</t>
</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
</ul>
</section>
</section>

</middle>

<back>

<section anchor="full-token-example-three-chain-governance"><name>Full Token Example (Three-Chain Governance)</name>

<sourcecode type="json"><![CDATA[{
  "iss": "https://auth.example.com",
  "sub": "user-alice",
  "aud": "https://api.example.com",
  "jti": "tok-fff-67890",
  "iat": 1700000000,
  "exp": 1700003600,

  "session": {
    "session_id": "sess-uuid-12345",
    "type": "human_initiated",
    "initiator": "user-alice",
    "approval_ref": "approval-uuid-789",
    "max_chain_depth": 5
  },

  "actor_chain": [
    {
      "sub":
        "spiffe://example.com/agent/orchestrator",
      "iss": "https://auth.example.com",
      "iat": 1700000010,
      "scope": "ticket:*",
      "chain_digest": "sha256:aaa...",
      "chain_sig": "eyJhbGci..."
    },
    {
      "sub":
        "spiffe://example.com/agent/analyst",
      "iss": "https://auth.example.com",
      "iat": 1700000030,
      "scope": "analysis:run",
      "chain_digest": "sha256:bbb...",
      "chain_sig": "eyJhbGci..."
    }
  ],

  "intent_root": "sha256:abc123...",
  "intent_registry":
    "https://intent-log.example.com/sessions/sess-uuid-12345",

  "inference_root": "sha256:xyz789...",
  "inference_proof_type": "tee_h100",
  "inference_registry":
    "https://proof-log.example.com/sessions/sess-uuid-12345"
}
]]></sourcecode>
</section>

</back>

</rfc>
